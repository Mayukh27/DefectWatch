# Backend environment template
# Copy to backend/.env and adjust as needed.

# Frontend origin(s) allowed by CORS (comma-separated)
CORS_ORIGINS=address_frontend_origins_here

# Optional defaults
DEFAULT_METHOD=custom
CAMERA_MODE=mixed
USE_DL=false
